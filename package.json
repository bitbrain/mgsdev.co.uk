{
  "name": "mgsdev.co.uk",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:ssg:bundle": "vite build --ssr src/ssg.ts --outDir dist-ssg",
    "build:ssg": "node ssg.mjs",
    "build": "npm run build:ssg:bundle && npm run build:ssg",
    "preview": "npx --yes serve dist"
  },
  "dependencies": {
    "@geajs/core": "^1.3.0"
  },
  "devDependencies": {
    "@geajs/vite-plugin": "^1.3.0",
    "linkedom": "^0.18.12",
    "typescript": "^5.6.3",
    "vite": "^8.0.0"
  }
}
